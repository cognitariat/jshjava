
1. 내가 만든다. => class를 직접 만드는 행위. 그러나 흔치는 않음
2. 가서 산다. (원형 -> 복사)
   => "new" 라는 명령어를 통해 jdk에 들어있는 수백개의 원형을 가져옴
3. 상속
   => 기존 부품의 재사용
   집(초록색 지붕) --> 갈색으로 바꾸자!("재정의(overwrite? override!)")

만약
card.kb.com이라는 주소가 있다면

com 폴더 아래 kb 폴더 아래 card 폴더가 있을 것


